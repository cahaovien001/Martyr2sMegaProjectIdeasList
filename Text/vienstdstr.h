#ifndef VIEN_STD_STR
#define VIEN_STD_STR

#include <stdio.h>
#include <string.h>
#include <ctype.h>

int isEven(int n);
int isOdd(int n);
char* lowerstr(char* string);
char* strtrm(char* string);
char* reverse(char* string);
char* shiftstr(char* string, int size, int n_move);

#endif /* VIEN_STD_STR */
